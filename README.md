# ssh-based.tidb.ticat
A [ticat](https://github.com/innerr/ticat) modules repo for TiDB cluster, for some ssh-based modules

This is dirty codes for cluster operations, shoud re-write later.

## Apply this repo by running:
```bash
$> ticat hub.add innerr/ssh-bashed.tidb.ticat
```

## Progress
```
***--  Host operating: raw-backup
-----      Parallel raw-backup
-----  Deploy: auto-configuring
```
